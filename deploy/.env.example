# 复制为 .env 按需覆盖。端口默认已避开现有项目占用。
PG_USER=authz
PG_PASSWORD=authz_dev_pw
SPICEDB_KEY=authz_dev_key

# 端口(宿主机侧)
SPICEDB_PG_PORT=15432
SPICEDB_GRPC_PORT=50051
SPICEDB_HTTP_PORT=8543
SPICEDB_METRICS_PORT=9099
CASDOOR_PORT=8000
PORTAL_PORT=5274
